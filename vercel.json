{
  "framework": null,
  "installCommand": "git submodule update --init --recursive",
  "buildCommand": "zola build",
  "outputDirectory": "public",
  "rewrites": [
    {
      "source": "/sert/:path*",
      "destination": "https://jahamars.github.io/sert/:path*"
    }
  ]
}